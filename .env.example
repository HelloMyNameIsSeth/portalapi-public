# Example environment file — replace placeholders with your own values.
# IMPORTANT: Do NOT commit real credentials to source control.
ENV=development

PORT=3000

# Database connection placeholders
DB_HOST=db_host
DB_USERNAME=db_username
DB_PASSWORD=CHANGE_ME
DATABASE=database

# JWT secret placeholder (change to a strong secret in production)
JWT_SECRET=CHANGE_ME

# RPC / node URLs
INFURA_NODE_URL=infura_node_url