TZ=UTC
NODE_ENV=development
PORT=5000

# Devcontainer env placeholders — replace with local values, do NOT commit secrets
DB_HOST=localhost
DB_USERNAME=CHANGE_ME
DB_PASSWORD=CHANGE_ME
DATABASE=portal_dev

# Dev-only JWT placeholder; do not use in production
JWT_SECRET=CHANGE_ME